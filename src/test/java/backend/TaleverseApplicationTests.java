package backend;

/**
 * Placeholder test class. The TaleVerse backend uses a pure Java HTTP server
 * (no Spring Boot at runtime). Connectivity is verified by running run.bat.
 */
public class TaleverseApplicationTests {
    // No tests needed here — backend is validated by run.bat startup output.
}

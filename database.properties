# MySQL catalog (database) used for books AND admin/auth when database.name is set.
# Matches a single-schema setup. Remove or comment out to use defaults: taleverse + taleverse_auth.
database.name=taleversedatabase

# Optional — leave commented if MySQL root has no password (same as env TALEVERSE_DB_PASSWORD)
mysql.password=India@22
